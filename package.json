{
  "scripts": {
    "build:puzzle": "cd puzzle && vite build",
    "build:app": "cd app && vite build",
    "deploy:app": "cd app && npm run deploy",
    "deploy": "npm run build:puzzle && npm run deploy:app"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.0.0",
    "gh-pages": "^6.3.0"
  }
}